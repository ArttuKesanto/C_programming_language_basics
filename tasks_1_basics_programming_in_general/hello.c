#include <stdio.h> // Testing running and compiling C. Basic Hello World, installed VSC deps. Compiling with gcc -o "SOMETHING" <FILENAME>.
                // With Code Runner -extension, simply type CTRL + ALT + N, and the compiling and running will happen instantly.

int main(void) {
    printf("Hello WORLD!\n");
    printf("");
    return 0; // Exiting the program.
}